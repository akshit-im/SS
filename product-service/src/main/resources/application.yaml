server:
  port: ${services.product.port}
spring:
  application:
    name: product-service
    label: ${spring.application.name}
    base-package: ${services.product.packages}
upload:
  path: /test
eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.client.serviceUrl.defaultZone}
management:
  zipkin:
    tracing:
      endpoint: ${spring.zipkin.baseUrl}