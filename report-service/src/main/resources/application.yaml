server:
  port: ${services.report.port}
spring:
  application:
    name: report-service
    label: ${spring.application.name}
    base-package: ${services.report.packages}
upload:
  path: /test
eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.client.serviceUrl.defaultZone}
management:
  zipkin:
    tracing:
      endpoint: ${spring.zipkin.baseUrl}