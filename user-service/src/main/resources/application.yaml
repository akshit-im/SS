server:
  port: ${services.user.port}
spring:
  application:
    name: user-service
    label: ${spring.application.name}
    base-package: ${services.user.packages}
upload:
  path: /test
eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.client.serviceUrl.defaultZone}
management:
  zipkin:
    tracing:
      endpoint: ${spring.zipkin.baseUrl}