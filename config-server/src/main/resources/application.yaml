server:
  port: 80
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://gitlab.com/JMaverick/config-server
          username: sudeepsingh.np@gmail.coom
          password: glpat-MfLRbdQnLKiYAZhHQ4UW
  zipkin:
    baseUrl: http://zipkin.test.com:9411/api/v2/spans
eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery.test.com/eureka
management:
  zipkin:
    tracing:
      endpoint: ${spring.zipkin.baseUrl}