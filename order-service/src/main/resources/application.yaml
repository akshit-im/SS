server:
  port: ${services.order.port}
spring:
  application:
    name: order-service
    label: ${spring.application.name}
    base-package: ${services.order.packages}
upload:
  path: /test
eureka:
  client:
    serviceUrl:
      defaultZone: ${eureka.client.serviceUrl.defaultZone}
management:
  zipkin:
    tracing:
      endpoint: ${spring.zipkin.baseUrl}